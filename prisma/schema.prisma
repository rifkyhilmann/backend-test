
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:Rifkyhilman123@db.efxzajrjxigjxwhodejx.supabase.co:5432/postgres"
}

enum PostStatus {
  DRAFT
  PUBLISHED
  ARCHIVED
}

model Post {
  id        String     @id @default(uuid())  // UUID sebagai ID unik
  title     String
  content   String
  status    PostStatus @default(DRAFT)      // Status default adalah DRAFT
  createdAt DateTime   @default(now())
}
